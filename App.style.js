import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  header: {
    backgroundColor: "#444",
    height: 50,
    justifyContent: "center",
    alignItems: "center",
  },
  body: { flex: 1 },
  footer: {
    backgroundColor: "#444",
    height: 60,
    justifyContent: "center",
    alignItems: "center",
  },
});